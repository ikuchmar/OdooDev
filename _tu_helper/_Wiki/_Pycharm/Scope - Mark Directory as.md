Scope - Mark Directory as

В PyCharm это делается через Project View → Scope (или через “Mark Directory as …”, если хочешь скрывать целые папки).

Вариант 1 (самый удобный): Scope в окне Project

Открой окно Project (обычно слева) — Alt + 1

Вверху окна Project найди выпадашку Scope (часто там стоит Project / All).

Выбери:

Open Files — покажет только открытые файлы

Current File — только текущий файл (и иногда его окружение)

Changed Files — только изменённые (если подключён Git)

или создай свой Scope (см. ниже)

Сделать свой “фильтр” (Custom Scope)

Settings → Appearance & Behavior → Scopes

Нажми + → Local Scope

Выдели нужные папки/файлы в дереве и добавь их в scope

Назови, например: WORK

Вернись в Project View и выбери этот scope в выпадашке Scope

✅ Итог: в дереве проекта будут видны только выбранные папки/файлы.

Вариант 2: Скрыть лишние папки (Mark Directory as Excluded)

Если есть папки, которые почти никогда не нужны (например venv, dist, build, node_modules, out):

ПКМ по папке в Project

Mark Directory as → Excluded

🔹 Папка исчезнет из индексации и почти везде перестанет мешать (и PyCharm станет быстрее).

⚠️ Если потом нужно вернуть:

ПКМ → Mark Directory as → Cancel Exclusion

Вариант 3: “Закрепить” рабочие папки сверху (Favorites / Bookmarks)

Если хочется не скрывать проект, а сделать “быстрый список рабочих папок”:

Favorites (в некоторых версиях) или Bookmarks

ПКМ по папке → Add to Favorites / Add Bookmark