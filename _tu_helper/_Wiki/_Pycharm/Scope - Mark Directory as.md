# Scope / Mark Directory as

Кратко: через Project View можно либо фильтровать дерево (Scope), либо скрывать папки (Excluded), либо закреплять избранное (Bookmarks/Favorites).

## Вариант 1. Быстрый выбор Scope в Project (рекомендуется)
1. Открыть окно Project — `Alt+1`.
2. В верхнем выпадающем списке Scope выбрать нужный режим:
   - `Open Files` — только открытые файлы.
   - `Current File` — текущий файл (и его окружение).
   - `Changed Files` — изменённые файлы (при подключённом Git).
   - Свой Scope (см. ниже).

### Как сделать свой Scope (фильтр)
1. `Settings → Appearance & Behavior → Scopes`.
2. Нажать `+` → `Local Scope`.
3. Отметить нужные папки/файлы в дереве и добавить в Scope.
4. Назвать, например: `WORK`.
5. В Project View выбрать этот Scope в выпадашке.

✅ Результат: в дереве проекта видны только выбранные папки/файлы.

## Вариант 2. Скрыть лишние папки (Mark Directory as → Excluded)
1. ПКМ по папке в Project.
2. `Mark Directory as → Excluded`.

🔹 Папка исключается из индексации, перестаёт мешать (ускоряет PyCharm).

Возврат: ПКМ → `Mark Directory as → Cancel Exclusion`.

## Вариант 3. Закрепить рабочие папки (Favorites / Bookmarks)
1. ПКМ по папке или файлу.
2. `Add to Favorites` / `Add Bookmark`.

🔹 Получается быстрый список рабочих папок/файлов без скрытия остального.

## Шпаргалка
- Показывать только нужное в дереве: выбери Scope в Project.
- Скрыть мусорные папки (venv, dist, build, node_modules, out): Mark Directory as → Excluded.
- Вернуть исключённые: Mark Directory as → Cancel Exclusion.
- Сделать свой фильтр: Settings → Scopes → + Local Scope → выбрать папки → выбрать Scope в Project.
- Быстрый доступ без скрытия: Add to Favorites / Bookmarks.
